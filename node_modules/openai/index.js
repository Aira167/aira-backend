class OpenAI {
  constructor(config) {
    this.config = config;
    this.chat = {
      completions: {
        create: async () => ({ choices: [{ message: { content: 'stub' } }] }),
      },
    };
  }
}
module.exports = OpenAI;
